- name: Playbook para criar inventário do Windows
  hosts: 172.20.33.26
  gather_facts: no

  tasks:
    - include_vars: users.yml

    - name: Create Multi User in OU ou=test,dc=200dev,dc=local
      microsoft.ad.user:
        name: var_name={{ item.name }}
        password: B0bP4ssw0rd
        state: present
        path: var_path={{ item.path }}
        groups:
          set:
          - Domain Admins
          - Domain Users
        sam_account_name: var_sam_account_name={{ item.sam_account_name }}
        display_name: var_display_name={{ item.display_name }}
        firstname: var_firstname={{ item.firstname }}
        lastname: var_lastname={{ item.lastname }}
      with_items: '{{users}}'    
